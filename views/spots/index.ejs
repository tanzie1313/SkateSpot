<!-- views/applications/index.ejs -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Your Skatespots</title>
  </head>
  <body>
    <!-- views/applications/index.ejs -->

<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Your Skatespots</h1>
    <a href="/users/<%=user._id%>/spots/new">Add an Skate Spot</a>
    <ul>
      <% spots.forEach((spot)=>{ %>
        <li>
          <!-- Add a link around each application, directing to the 'show' page -->
          <a href="/users/<%= user._id %>/spots/<%= spot._id %>">
            <%= spot.name %> at <%= spot.location %>
          </a>
        </li>
        <% }) %>
      </ul>
        
  </body>
  
   
    </body>
</html>
